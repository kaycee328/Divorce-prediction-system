# from .models import Divorce
# from django.db import models
# from django.db.models import Q


# class DpsManager(models.Manager):
#     def for_user(self, user):
#         return self.get_queryset().filter(user=user)
